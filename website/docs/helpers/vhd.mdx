---
id: vhd
title: Visually hidden
slug: visually-hidden
---

Simply hide an element to screen, but still exposed to assistive technologies
(such as screen readers) with the `.h-vhd` class.

```html
<p class="h-vhd">Title for screen readers</p>
```
